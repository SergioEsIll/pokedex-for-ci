name: Say Hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the messsage
        run: |
          echo "Hola Sergio!!"
      - name: Dime la fecha
        run: date
      - run: ls -ls
